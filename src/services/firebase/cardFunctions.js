import Manager from './Manager';
import firebase from './firebase';

export const addCard = (title, content) => {};

export const updateCard = (propsToUpdate, cardId) => {};

export const deleteCard = (cardId, onComplete = () => {}) => {};
